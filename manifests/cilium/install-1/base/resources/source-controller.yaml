apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: cilium
  namespace: flux-system
spec:
  interval: 12h
  url: oci://quay.io/cilium/charts/cilium
  ref:
    semver: "1.19.x"
