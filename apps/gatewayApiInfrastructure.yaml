apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infrastructure-gatewayapi
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "90"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/hausteen/kubernetesApplications.git
    path: gatewayApi/overlays/infrastructure
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
    syncOptions:
      - ServerSideApply=true
