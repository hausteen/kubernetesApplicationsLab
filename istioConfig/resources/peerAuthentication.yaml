# apiVersion: security.istio.io/v1
# kind: PeerAuthentication
# metadata:
#   name: mtls-mode
#   namespace: istio-system
# spec:
#   mtls:
#     mode: STRICT

# this is causing the various webhooks in the system to fail.
# i'm not sure why those webhooks are failing right now.
